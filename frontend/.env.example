NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
# Origin shown in local/CI scan setup as `--server <origin>` (no /api/v1 suffix).
# In production behind Caddy this is the portal's public origin.
NEXT_PUBLIC_PORTAL_ORIGIN=http://localhost:8000
